import React from 'react';
import Artwork from './Artwork';

const Gallery = () => {
    return (
        <section className="gallery">
            {/* Render your artworks using the Artwork component */}
        </section>
    );
};

export default Gallery;
